# Backend Configuration
# ====================

# Database URL - supports SQLite and PostgreSQL
# SQLite (default - good for development):
DATABASE_URL=sqlite:///./transcripts.db

# PostgreSQL (for production with concurrent access):
# DATABASE_URL=postgresql://user:password@localhost:5432/parakeet

# Authentication Configuration
# =============================

# JWT Secret Key for token signing (change this in production!)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-me-in-production

# Google OAuth Configuration
# Get your Client ID from https://console.cloud.google.com
# WARNING: Do NOT put the Client Secret here - it's not needed for ID Token verification
GOOGLE_CLIENT_ID=your_google_client_id_here

# Optional: Hugging Face token for improved pyannote.audio model access
# HUGGINGFACE_TOKEN=your_token_here

# Frontend Configuration
# =====================

# API URL for frontend to communicate with backend
# Docker: http://backend:8000 (internal network)
# Local dev: http://localhost:8000
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://backend:8000

# Google OAuth Client ID (same as backend GOOGLE_CLIENT_ID)
# Get your credentials from https://console.cloud.google.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here

# Optional: Set to 'true' to enable debug mode
NEXT_PUBLIC_DEBUG=false

# Docker Compose Configuration
# =============================

# Set to 'true' to enable GPU support (requires nvidia-docker)
# See DOCKER_GPU_SETUP.md for setup instructions
ENABLE_GPU=false

# GPU device ID (if multiple GPUs available)
CUDA_VISIBLE_DEVICES=0
